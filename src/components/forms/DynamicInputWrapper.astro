---
// Import → CSS
import "@form-styles/dynamic-input.css";

interface Props {
    trigger: string;
}

const { trigger } = Astro.props;
---

<div class="dynamic-input" data-trigger={trigger}>
    <slot />
</div>

