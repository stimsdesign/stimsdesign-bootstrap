---
// Custom TextInput Component

import TextInput from "./TextInput.astro";

interface Props {
    id: string;
    name?: string;
    label?: string;
    required?: boolean;
    [key: string]: any;
}

const { 
    id, 
    name = id,
    label = "Full Name", 
    required = false, 
    ...rest 
} = Astro.props;
---

<TextInput 
    id={id} 
    name={name} 
    label={label} 
    required={required} 
    autocomplete="name" 
    {...rest} 
/>
