---
// Custom TextInput Component

import TextInput from "./TextInput.astro";

const { id = "address", label = "Street Address", ...rest } = Astro.props;
---
<TextInput id={id} label={label} autocomplete="address-line1" {...rest} />
